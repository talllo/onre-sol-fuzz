//! # Trident Generated Types
//!
//! This file is automatically generated by Trident.
//! **DO NOT EDIT THIS FILE MANUALLY**

#![allow(dead_code)]
#![allow(unused_imports)]

// Ensure exactly one network feature is active (must match the program build)
#[cfg(all(feature = "mainnet", feature = "mainnet-test"))]
compile_error!("'mainnet' and 'mainnet-test' features are mutually exclusive");
#[cfg(all(feature = "mainnet", feature = "devnet-test"))]
compile_error!("'mainnet' and 'devnet-test' features are mutually exclusive");
#[cfg(all(feature = "mainnet", feature = "devnet-dev"))]
compile_error!("'mainnet' and 'devnet-dev' features are mutually exclusive");
#[cfg(all(feature = "mainnet-test", feature = "devnet-test"))]
compile_error!("'mainnet-test' and 'devnet-test' features are mutually exclusive");
#[cfg(all(feature = "mainnet-test", feature = "devnet-dev"))]
compile_error!("'mainnet-test' and 'devnet-dev' features are mutually exclusive");
#[cfg(all(feature = "devnet-test", feature = "devnet-dev"))]
compile_error!("'devnet-test' and 'devnet-dev' features are mutually exclusive");

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use trident_fuzz::fuzzing::*;

// ============================================================================
// PROGRAM MODULES
// ============================================================================

// ----------------------------------------------------------------------------
// Program: onreapp
// ----------------------------------------------------------------------------
pub mod onreapp {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for onreapp based on the active feature flag.
    /// Must match the feature used when building the program binary.
    pub fn program_id() -> Pubkey {
        #[cfg(feature = "mainnet")]
        { pubkey!("onreuGhHHgVzMWSkj2oQDLDtvvGvoepBPkqyaubFcwe") }

        #[cfg(feature = "mainnet-test")]
        { pubkey!("J24jWEosQc5jgkdPm3YzNgzQ54CqNKkhzKy56XXJsLo2") }

        #[cfg(feature = "devnet-test")]
        { pubkey!("J24jWEosQc5jgkdPm3YzNgzQ54CqNKkhzKy56XXJsLo2") }

        #[cfg(feature = "devnet-dev")]
        { pubkey!("devHfQHgiFNifkLW49RCXpyTUZMyKuBNnFSbrQ8XsbX") }
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AcceptBoss
    // ....................................................................

    /// Main instruction struct for AcceptBoss
    pub struct AcceptBossInstruction {
        pub accounts: AcceptBossInstructionAccountMetas,
        pub data: AcceptBossInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AcceptBoss instruction
    #[derive(Debug, Clone, Default)]
    pub struct AcceptBossInstructionAccountMetas {
        pub state: AccountMeta,

        pub new_boss: AccountMeta,
    }

    /// Account pubkeys for AcceptBoss instruction
    #[derive(Debug, Clone)]
    pub struct AcceptBossInstructionAccounts {
        pub state: Pubkey,

        pub new_boss: Pubkey,
    }

    impl AcceptBossInstructionAccounts {
        pub fn new(state: Pubkey, new_boss: Pubkey) -> Self {
            Self { state, new_boss }
        }
    }

    /// Instruction data for AcceptBoss
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AcceptBossInstructionData {}

    impl AcceptBossInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for AcceptBossInstruction
    impl AcceptBossInstruction {
        fn discriminator() -> [u8; 8] {
            [152u8, 63u8, 117u8, 209u8, 67u8, 11u8, 250u8, 242u8]
        }

        pub fn data(data: AcceptBossInstructionData) -> Self {
            Self {
                accounts: AcceptBossInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AcceptBossInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.new_boss = AccountMeta::new_readonly(accounts.new_boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.new_boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: AddAdmin
    // ....................................................................

    /// Main instruction struct for AddAdmin
    pub struct AddAdminInstruction {
        pub accounts: AddAdminInstructionAccountMetas,
        pub data: AddAdminInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AddAdmin instruction
    #[derive(Debug, Clone, Default)]
    pub struct AddAdminInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for AddAdmin instruction
    #[derive(Debug, Clone)]
    pub struct AddAdminInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl AddAdminInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey) -> Self {
            Self { state, boss }
        }
    }

    /// Instruction data for AddAdmin
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AddAdminInstructionData {
        pub new_admin: Pubkey,
    }

    impl AddAdminInstructionData {
        pub fn new(new_admin: Pubkey) -> Self {
            Self { new_admin }
        }
    }

    /// Implementation for AddAdminInstruction
    impl AddAdminInstruction {
        fn discriminator() -> [u8; 8] {
            [177u8, 236u8, 33u8, 205u8, 124u8, 152u8, 55u8, 186u8]
        }

        pub fn data(data: AddAdminInstructionData) -> Self {
            Self {
                accounts: AddAdminInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AddAdminInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: AddApprover
    // ....................................................................

    /// Main instruction struct for AddApprover
    pub struct AddApproverInstruction {
        pub accounts: AddApproverInstructionAccountMetas,
        pub data: AddApproverInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AddApprover instruction
    #[derive(Debug, Clone, Default)]
    pub struct AddApproverInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for AddApprover instruction
    #[derive(Debug, Clone)]
    pub struct AddApproverInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl AddApproverInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey) -> Self {
            Self { state, boss }
        }
    }

    /// Instruction data for AddApprover
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AddApproverInstructionData {
        pub approver: Pubkey,
    }

    impl AddApproverInstructionData {
        pub fn new(approver: Pubkey) -> Self {
            Self { approver }
        }
    }

    /// Implementation for AddApproverInstruction
    impl AddApproverInstruction {
        fn discriminator() -> [u8; 8] {
            [213u8, 245u8, 135u8, 79u8, 129u8, 129u8, 22u8, 80u8]
        }

        pub fn data(data: AddApproverInstructionData) -> Self {
            Self {
                accounts: AddApproverInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AddApproverInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: AddOfferVector
    // ....................................................................

    /// Main instruction struct for AddOfferVector
    pub struct AddOfferVectorInstruction {
        pub accounts: AddOfferVectorInstructionAccountMetas,
        pub data: AddOfferVectorInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AddOfferVector instruction
    #[derive(Debug, Clone, Default)]
    pub struct AddOfferVectorInstructionAccountMetas {
        pub offer: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_out_mint: AccountMeta,

        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for AddOfferVector instruction
    #[derive(Debug, Clone)]
    pub struct AddOfferVectorInstructionAccounts {
        pub offer: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_out_mint: Pubkey,

        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl AddOfferVectorInstructionAccounts {
        pub fn new(
            offer: Pubkey,

            token_in_mint: Pubkey,

            token_out_mint: Pubkey,

            state: Pubkey,

            boss: Pubkey,
        ) -> Self {
            Self {
                offer,

                token_in_mint,

                token_out_mint,

                state,

                boss,
            }
        }
    }

    /// Instruction data for AddOfferVector
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AddOfferVectorInstructionData {
        pub start_time: Option<u64>,

        pub base_time: u64,

        pub base_price: u64,

        pub apr: u64,

        pub price_fix_duration: u64,
    }

    impl AddOfferVectorInstructionData {
        pub fn new(
            start_time: Option<u64>,

            base_time: u64,

            base_price: u64,

            apr: u64,

            price_fix_duration: u64,
        ) -> Self {
            Self {
                start_time,

                base_time,

                base_price,

                apr,

                price_fix_duration,
            }
        }
    }

    /// Implementation for AddOfferVectorInstruction
    impl AddOfferVectorInstruction {
        fn discriminator() -> [u8; 8] {
            [198u8, 139u8, 180u8, 6u8, 156u8, 171u8, 188u8, 61u8]
        }

        pub fn data(data: AddOfferVectorInstructionData) -> Self {
            Self {
                accounts: AddOfferVectorInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AddOfferVectorInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new(accounts.offer, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClearAdmins
    // ....................................................................

    /// Main instruction struct for ClearAdmins
    pub struct ClearAdminsInstruction {
        pub accounts: ClearAdminsInstructionAccountMetas,
        pub data: ClearAdminsInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClearAdmins instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClearAdminsInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for ClearAdmins instruction
    #[derive(Debug, Clone)]
    pub struct ClearAdminsInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl ClearAdminsInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey) -> Self {
            Self { state, boss }
        }
    }

    /// Instruction data for ClearAdmins
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClearAdminsInstructionData {}

    impl ClearAdminsInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ClearAdminsInstruction
    impl ClearAdminsInstruction {
        fn discriminator() -> [u8; 8] {
            [39u8, 200u8, 132u8, 30u8, 196u8, 160u8, 73u8, 55u8]
        }

        pub fn data(data: ClearAdminsInstructionData) -> Self {
            Self {
                accounts: ClearAdminsInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClearAdminsInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CloseOffer
    // ....................................................................

    /// Main instruction struct for CloseOffer
    pub struct CloseOfferInstruction {
        pub accounts: CloseOfferInstructionAccountMetas,
        pub data: CloseOfferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CloseOffer instruction
    #[derive(Debug, Clone, Default)]
    pub struct CloseOfferInstructionAccountMetas {
        pub offer: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_out_mint: AccountMeta,

        pub boss: AccountMeta,

        pub state: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CloseOffer instruction
    #[derive(Debug, Clone)]
    pub struct CloseOfferInstructionAccounts {
        pub offer: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_out_mint: Pubkey,

        pub boss: Pubkey,

        pub state: Pubkey,
    }

    impl CloseOfferInstructionAccounts {
        pub fn new(
            offer: Pubkey,

            token_in_mint: Pubkey,

            token_out_mint: Pubkey,

            boss: Pubkey,

            state: Pubkey,
        ) -> Self {
            Self {
                offer,

                token_in_mint,

                token_out_mint,

                boss,

                state,
            }
        }
    }

    /// Instruction data for CloseOffer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CloseOfferInstructionData {}

    impl CloseOfferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for CloseOfferInstruction
    impl CloseOfferInstruction {
        fn discriminator() -> [u8; 8] {
            [191u8, 72u8, 67u8, 35u8, 239u8, 209u8, 97u8, 132u8]
        }

        pub fn data(data: CloseOfferInstructionData) -> Self {
            Self {
                accounts: CloseOfferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CloseOfferInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new(accounts.offer, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CloseState
    // ....................................................................

    /// Main instruction struct for CloseState
    pub struct CloseStateInstruction {
        pub accounts: CloseStateInstructionAccountMetas,
        pub data: CloseStateInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CloseState instruction
    #[derive(Debug, Clone, Default)]
    pub struct CloseStateInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CloseState instruction
    #[derive(Debug, Clone)]
    pub struct CloseStateInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl CloseStateInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey) -> Self {
            Self { state, boss }
        }
    }

    /// Instruction data for CloseState
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CloseStateInstructionData {}

    impl CloseStateInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for CloseStateInstruction
    impl CloseStateInstruction {
        fn discriminator() -> [u8; 8] {
            [25u8, 1u8, 184u8, 101u8, 200u8, 245u8, 210u8, 246u8]
        }

        pub fn data(data: CloseStateInstructionData) -> Self {
            Self {
                accounts: CloseStateInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CloseStateInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new(accounts.boss, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ConfigureMaxSupply
    // ....................................................................

    /// Main instruction struct for ConfigureMaxSupply
    pub struct ConfigureMaxSupplyInstruction {
        pub accounts: ConfigureMaxSupplyInstructionAccountMetas,
        pub data: ConfigureMaxSupplyInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ConfigureMaxSupply instruction
    #[derive(Debug, Clone, Default)]
    pub struct ConfigureMaxSupplyInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for ConfigureMaxSupply instruction
    #[derive(Debug, Clone)]
    pub struct ConfigureMaxSupplyInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl ConfigureMaxSupplyInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey) -> Self {
            Self { state, boss }
        }
    }

    /// Instruction data for ConfigureMaxSupply
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ConfigureMaxSupplyInstructionData {
        pub max_supply: u64,
    }

    impl ConfigureMaxSupplyInstructionData {
        pub fn new(max_supply: u64) -> Self {
            Self { max_supply }
        }
    }

    /// Implementation for ConfigureMaxSupplyInstruction
    impl ConfigureMaxSupplyInstruction {
        fn discriminator() -> [u8; 8] {
            [145u8, 100u8, 133u8, 229u8, 142u8, 59u8, 96u8, 62u8]
        }

        pub fn data(data: ConfigureMaxSupplyInstructionData) -> Self {
            Self {
                accounts: ConfigureMaxSupplyInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ConfigureMaxSupplyInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: DeleteOfferVector
    // ....................................................................

    /// Main instruction struct for DeleteOfferVector
    pub struct DeleteOfferVectorInstruction {
        pub accounts: DeleteOfferVectorInstructionAccountMetas,
        pub data: DeleteOfferVectorInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for DeleteOfferVector instruction
    #[derive(Debug, Clone, Default)]
    pub struct DeleteOfferVectorInstructionAccountMetas {
        pub offer: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_out_mint: AccountMeta,

        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for DeleteOfferVector instruction
    #[derive(Debug, Clone)]
    pub struct DeleteOfferVectorInstructionAccounts {
        pub offer: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_out_mint: Pubkey,

        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl DeleteOfferVectorInstructionAccounts {
        pub fn new(
            offer: Pubkey,

            token_in_mint: Pubkey,

            token_out_mint: Pubkey,

            state: Pubkey,

            boss: Pubkey,
        ) -> Self {
            Self {
                offer,

                token_in_mint,

                token_out_mint,

                state,

                boss,
            }
        }
    }

    /// Instruction data for DeleteOfferVector
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct DeleteOfferVectorInstructionData {
        pub vector_start_time: u64,
    }

    impl DeleteOfferVectorInstructionData {
        pub fn new(vector_start_time: u64) -> Self {
            Self { vector_start_time }
        }
    }

    /// Implementation for DeleteOfferVectorInstruction
    impl DeleteOfferVectorInstruction {
        fn discriminator() -> [u8; 8] {
            [87u8, 40u8, 79u8, 151u8, 78u8, 121u8, 46u8, 159u8]
        }

        pub fn data(data: DeleteOfferVectorInstructionData) -> Self {
            Self {
                accounts: DeleteOfferVectorInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: DeleteOfferVectorInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new(accounts.offer, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: GetApy
    // ....................................................................

    /// Main instruction struct for GetApy
    pub struct GetApyInstruction {
        pub accounts: GetApyInstructionAccountMetas,
        pub data: GetApyInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for GetApy instruction
    #[derive(Debug, Clone, Default)]
    pub struct GetApyInstructionAccountMetas {
        pub offer: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_out_mint: AccountMeta,
    }

    /// Account pubkeys for GetApy instruction
    #[derive(Debug, Clone)]
    pub struct GetApyInstructionAccounts {
        pub offer: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_out_mint: Pubkey,
    }

    impl GetApyInstructionAccounts {
        pub fn new(offer: Pubkey, token_in_mint: Pubkey, token_out_mint: Pubkey) -> Self {
            Self {
                offer,

                token_in_mint,

                token_out_mint,
            }
        }
    }

    /// Instruction data for GetApy
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct GetApyInstructionData {}

    impl GetApyInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for GetApyInstruction
    impl GetApyInstruction {
        fn discriminator() -> [u8; 8] {
            [194u8, 123u8, 183u8, 54u8, 181u8, 74u8, 194u8, 97u8]
        }

        pub fn data(data: GetApyInstructionData) -> Self {
            Self {
                accounts: GetApyInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: GetApyInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new_readonly(accounts.offer, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: GetCirculatingSupply
    // ....................................................................

    /// Main instruction struct for GetCirculatingSupply
    pub struct GetCirculatingSupplyInstruction {
        pub accounts: GetCirculatingSupplyInstructionAccountMetas,
        pub data: GetCirculatingSupplyInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for GetCirculatingSupply instruction
    #[derive(Debug, Clone, Default)]
    pub struct GetCirculatingSupplyInstructionAccountMetas {
        pub onyc_mint: AccountMeta,

        pub state: AccountMeta,

        pub vault_authority: AccountMeta,

        pub onyc_vault_account: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for GetCirculatingSupply instruction
    #[derive(Debug, Clone)]
    pub struct GetCirculatingSupplyInstructionAccounts {
        pub onyc_mint: Pubkey,

        pub state: Pubkey,

        pub vault_authority: Pubkey,

        pub onyc_vault_account: Pubkey,

        pub token_program: Pubkey,
    }

    impl GetCirculatingSupplyInstructionAccounts {
        pub fn new(
            onyc_mint: Pubkey,

            state: Pubkey,

            vault_authority: Pubkey,

            onyc_vault_account: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                onyc_mint,

                state,

                vault_authority,

                onyc_vault_account,

                token_program,
            }
        }
    }

    /// Instruction data for GetCirculatingSupply
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct GetCirculatingSupplyInstructionData {}

    impl GetCirculatingSupplyInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for GetCirculatingSupplyInstruction
    impl GetCirculatingSupplyInstruction {
        fn discriminator() -> [u8; 8] {
            [132u8, 168u8, 96u8, 104u8, 217u8, 255u8, 111u8, 152u8]
        }

        pub fn data(data: GetCirculatingSupplyInstructionData) -> Self {
            Self {
                accounts: GetCirculatingSupplyInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: GetCirculatingSupplyInstructionAccounts) -> Self {
            self.accounts.onyc_mint = AccountMeta::new_readonly(accounts.onyc_mint, false);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.vault_authority =
                AccountMeta::new_readonly(accounts.vault_authority, false);

            self.accounts.onyc_vault_account =
                AccountMeta::new_readonly(accounts.onyc_vault_account, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.onyc_mint.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.vault_authority.clone());

            metas.push(self.accounts.onyc_vault_account.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: GetNav
    // ....................................................................

    /// Main instruction struct for GetNav
    pub struct GetNavInstruction {
        pub accounts: GetNavInstructionAccountMetas,
        pub data: GetNavInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for GetNav instruction
    #[derive(Debug, Clone, Default)]
    pub struct GetNavInstructionAccountMetas {
        pub offer: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_out_mint: AccountMeta,
    }

    /// Account pubkeys for GetNav instruction
    #[derive(Debug, Clone)]
    pub struct GetNavInstructionAccounts {
        pub offer: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_out_mint: Pubkey,
    }

    impl GetNavInstructionAccounts {
        pub fn new(offer: Pubkey, token_in_mint: Pubkey, token_out_mint: Pubkey) -> Self {
            Self {
                offer,

                token_in_mint,

                token_out_mint,
            }
        }
    }

    /// Instruction data for GetNav
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct GetNavInstructionData {}

    impl GetNavInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for GetNavInstruction
    impl GetNavInstruction {
        fn discriminator() -> [u8; 8] {
            [200u8, 89u8, 76u8, 53u8, 215u8, 218u8, 63u8, 21u8]
        }

        pub fn data(data: GetNavInstructionData) -> Self {
            Self {
                accounts: GetNavInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: GetNavInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new_readonly(accounts.offer, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: GetNavAdjustment
    // ....................................................................

    /// Main instruction struct for GetNavAdjustment
    pub struct GetNavAdjustmentInstruction {
        pub accounts: GetNavAdjustmentInstructionAccountMetas,
        pub data: GetNavAdjustmentInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for GetNavAdjustment instruction
    #[derive(Debug, Clone, Default)]
    pub struct GetNavAdjustmentInstructionAccountMetas {
        pub offer: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_out_mint: AccountMeta,
    }

    /// Account pubkeys for GetNavAdjustment instruction
    #[derive(Debug, Clone)]
    pub struct GetNavAdjustmentInstructionAccounts {
        pub offer: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_out_mint: Pubkey,
    }

    impl GetNavAdjustmentInstructionAccounts {
        pub fn new(offer: Pubkey, token_in_mint: Pubkey, token_out_mint: Pubkey) -> Self {
            Self {
                offer,

                token_in_mint,

                token_out_mint,
            }
        }
    }

    /// Instruction data for GetNavAdjustment
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct GetNavAdjustmentInstructionData {}

    impl GetNavAdjustmentInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for GetNavAdjustmentInstruction
    impl GetNavAdjustmentInstruction {
        fn discriminator() -> [u8; 8] {
            [70u8, 198u8, 229u8, 129u8, 238u8, 233u8, 143u8, 94u8]
        }

        pub fn data(data: GetNavAdjustmentInstructionData) -> Self {
            Self {
                accounts: GetNavAdjustmentInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: GetNavAdjustmentInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new_readonly(accounts.offer, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: GetTvl
    // ....................................................................

    /// Main instruction struct for GetTvl
    pub struct GetTvlInstruction {
        pub accounts: GetTvlInstructionAccountMetas,
        pub data: GetTvlInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for GetTvl instruction
    #[derive(Debug, Clone, Default)]
    pub struct GetTvlInstructionAccountMetas {
        pub offer: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_out_mint: AccountMeta,

        pub vault_authority: AccountMeta,

        pub vault_token_out_account: AccountMeta,

        pub token_out_program: AccountMeta,
    }

    /// Account pubkeys for GetTvl instruction
    #[derive(Debug, Clone)]
    pub struct GetTvlInstructionAccounts {
        pub offer: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_out_mint: Pubkey,

        pub vault_authority: Pubkey,

        pub vault_token_out_account: Pubkey,

        pub token_out_program: Pubkey,
    }

    impl GetTvlInstructionAccounts {
        pub fn new(
            offer: Pubkey,

            token_in_mint: Pubkey,

            token_out_mint: Pubkey,

            vault_authority: Pubkey,

            vault_token_out_account: Pubkey,

            token_out_program: Pubkey,
        ) -> Self {
            Self {
                offer,

                token_in_mint,

                token_out_mint,

                vault_authority,

                vault_token_out_account,

                token_out_program,
            }
        }
    }

    /// Instruction data for GetTvl
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct GetTvlInstructionData {}

    impl GetTvlInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for GetTvlInstruction
    impl GetTvlInstruction {
        fn discriminator() -> [u8; 8] {
            [88u8, 225u8, 219u8, 204u8, 86u8, 91u8, 184u8, 51u8]
        }

        pub fn data(data: GetTvlInstructionData) -> Self {
            Self {
                accounts: GetTvlInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: GetTvlInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new_readonly(accounts.offer, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self.accounts.vault_authority =
                AccountMeta::new_readonly(accounts.vault_authority, false);

            self.accounts.vault_token_out_account =
                AccountMeta::new_readonly(accounts.vault_token_out_account, false);

            self.accounts.token_out_program =
                AccountMeta::new_readonly(accounts.token_out_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.push(self.accounts.vault_authority.clone());

            metas.push(self.accounts.vault_token_out_account.clone());

            metas.push(self.accounts.token_out_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Initialize
    // ....................................................................

    /// Main instruction struct for Initialize
    pub struct InitializeInstruction {
        pub accounts: InitializeInstructionAccountMetas,
        pub data: InitializeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Initialize instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeInstructionAccountMetas {
        pub state: AccountMeta,

        pub mint_authority: AccountMeta,

        pub offer_vault_authority: AccountMeta,

        pub boss: AccountMeta,

        pub program: AccountMeta,

        pub program_data: AccountMeta,

        pub onyc_mint: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for Initialize instruction
    #[derive(Debug, Clone)]
    pub struct InitializeInstructionAccounts {
        pub state: Pubkey,

        pub mint_authority: Pubkey,

        pub offer_vault_authority: Pubkey,

        pub boss: Pubkey,

        pub program_data: Pubkey,

        pub onyc_mint: Pubkey,
    }

    impl InitializeInstructionAccounts {
        pub fn new(
            state: Pubkey,

            mint_authority: Pubkey,

            offer_vault_authority: Pubkey,

            boss: Pubkey,

            program_data: Pubkey,

            onyc_mint: Pubkey,
        ) -> Self {
            Self {
                state,

                mint_authority,

                offer_vault_authority,

                boss,

                program_data,

                onyc_mint,
            }
        }
    }

    /// Instruction data for Initialize
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeInstructionData {}

    impl InitializeInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializeInstruction
    impl InitializeInstruction {
        fn discriminator() -> [u8; 8] {
            [175u8, 175u8, 109u8, 31u8, 13u8, 152u8, 155u8, 237u8]
        }

        pub fn data(data: InitializeInstructionData) -> Self {
            Self {
                accounts: InitializeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.mint_authority = AccountMeta::new(accounts.mint_authority, false);

            self.accounts.offer_vault_authority =
                AccountMeta::new(accounts.offer_vault_authority, false);

            self.accounts.boss = AccountMeta::new(accounts.boss, true);

            self.accounts.program = AccountMeta::new_readonly(
                program_id(),
                false,
            );

            self.accounts.program_data = AccountMeta::new_readonly(accounts.program_data, false);

            self.accounts.onyc_mint = AccountMeta::new_readonly(accounts.onyc_mint, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.mint_authority.clone());

            metas.push(self.accounts.offer_vault_authority.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.program.clone());

            metas.push(self.accounts.program_data.clone());

            metas.push(self.accounts.onyc_mint.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializePermissionlessAuthority
    // ....................................................................

    /// Main instruction struct for InitializePermissionlessAuthority
    pub struct InitializePermissionlessAuthorityInstruction {
        pub accounts: InitializePermissionlessAuthorityInstructionAccountMetas,
        pub data: InitializePermissionlessAuthorityInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializePermissionlessAuthority instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializePermissionlessAuthorityInstructionAccountMetas {
        pub permissionless_authority: AccountMeta,

        pub state: AccountMeta,

        pub boss: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializePermissionlessAuthority instruction
    #[derive(Debug, Clone)]
    pub struct InitializePermissionlessAuthorityInstructionAccounts {
        pub permissionless_authority: Pubkey,

        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl InitializePermissionlessAuthorityInstructionAccounts {
        pub fn new(permissionless_authority: Pubkey, state: Pubkey, boss: Pubkey) -> Self {
            Self {
                permissionless_authority,

                state,

                boss,
            }
        }
    }

    /// Instruction data for InitializePermissionlessAuthority
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializePermissionlessAuthorityInstructionData {
        pub name: String,
    }

    impl InitializePermissionlessAuthorityInstructionData {
        pub fn new(name: String) -> Self {
            Self { name }
        }
    }

    /// Implementation for InitializePermissionlessAuthorityInstruction
    impl InitializePermissionlessAuthorityInstruction {
        fn discriminator() -> [u8; 8] {
            [89u8, 93u8, 43u8, 180u8, 148u8, 16u8, 238u8, 24u8]
        }

        pub fn data(data: InitializePermissionlessAuthorityInstructionData) -> Self {
            Self {
                accounts: InitializePermissionlessAuthorityInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(
            mut self,
            accounts: InitializePermissionlessAuthorityInstructionAccounts,
        ) -> Self {
            self.accounts.permissionless_authority =
                AccountMeta::new(accounts.permissionless_authority, false);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.boss = AccountMeta::new(accounts.boss, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.permissionless_authority.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: MakeOffer
    // ....................................................................

    /// Main instruction struct for MakeOffer
    pub struct MakeOfferInstruction {
        pub accounts: MakeOfferInstructionAccountMetas,
        pub data: MakeOfferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for MakeOffer instruction
    #[derive(Debug, Clone, Default)]
    pub struct MakeOfferInstructionAccountMetas {
        pub vault_authority: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_in_program: AccountMeta,

        pub vault_token_in_account: AccountMeta,

        pub token_out_mint: AccountMeta,

        pub offer: AccountMeta,

        pub state: AccountMeta,

        pub boss: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for MakeOffer instruction
    #[derive(Debug, Clone)]
    pub struct MakeOfferInstructionAccounts {
        pub vault_authority: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_in_program: Pubkey,

        pub vault_token_in_account: Pubkey,

        pub token_out_mint: Pubkey,

        pub offer: Pubkey,

        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl MakeOfferInstructionAccounts {
        pub fn new(
            vault_authority: Pubkey,

            token_in_mint: Pubkey,

            token_in_program: Pubkey,

            vault_token_in_account: Pubkey,

            token_out_mint: Pubkey,

            offer: Pubkey,

            state: Pubkey,

            boss: Pubkey,
        ) -> Self {
            Self {
                vault_authority,

                token_in_mint,

                token_in_program,

                vault_token_in_account,

                token_out_mint,

                offer,

                state,

                boss,
            }
        }
    }

    /// Instruction data for MakeOffer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MakeOfferInstructionData {
        pub fee_basis_points: u16,

        pub needs_approval: bool,

        pub allow_permissionless: bool,
    }

    impl MakeOfferInstructionData {
        pub fn new(
            fee_basis_points: u16,

            needs_approval: bool,

            allow_permissionless: bool,
        ) -> Self {
            Self {
                fee_basis_points,

                needs_approval,

                allow_permissionless,
            }
        }
    }

    /// Implementation for MakeOfferInstruction
    impl MakeOfferInstruction {
        fn discriminator() -> [u8; 8] {
            [214u8, 98u8, 97u8, 35u8, 59u8, 12u8, 44u8, 178u8]
        }

        pub fn data(data: MakeOfferInstructionData) -> Self {
            Self {
                accounts: MakeOfferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MakeOfferInstructionAccounts) -> Self {
            self.accounts.vault_authority =
                AccountMeta::new_readonly(accounts.vault_authority, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_in_program =
                AccountMeta::new_readonly(accounts.token_in_program, false);

            self.accounts.vault_token_in_account =
                AccountMeta::new(accounts.vault_token_in_account, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self.accounts.offer = AccountMeta::new(accounts.offer, false);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.boss = AccountMeta::new(accounts.boss, true);

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.vault_authority.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_in_program.clone());

            metas.push(self.accounts.vault_token_in_account.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: MintTo
    // ....................................................................

    /// Main instruction struct for MintTo
    pub struct MintToInstruction {
        pub accounts: MintToInstructionAccountMetas,
        pub data: MintToInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for MintTo instruction
    #[derive(Debug, Clone, Default)]
    pub struct MintToInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,

        pub onyc_mint: AccountMeta,

        pub boss_onyc_account: AccountMeta,

        pub mint_authority: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for MintTo instruction
    #[derive(Debug, Clone)]
    pub struct MintToInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,

        pub onyc_mint: Pubkey,

        pub boss_onyc_account: Pubkey,

        pub mint_authority: Pubkey,

        pub token_program: Pubkey,
    }

    impl MintToInstructionAccounts {
        pub fn new(
            state: Pubkey,

            boss: Pubkey,

            onyc_mint: Pubkey,

            boss_onyc_account: Pubkey,

            mint_authority: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                state,

                boss,

                onyc_mint,

                boss_onyc_account,

                mint_authority,

                token_program,
            }
        }
    }

    /// Instruction data for MintTo
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MintToInstructionData {
        pub amount: u64,
    }

    impl MintToInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for MintToInstruction
    impl MintToInstruction {
        fn discriminator() -> [u8; 8] {
            [241u8, 34u8, 48u8, 186u8, 37u8, 179u8, 123u8, 192u8]
        }

        pub fn data(data: MintToInstructionData) -> Self {
            Self {
                accounts: MintToInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MintToInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.boss = AccountMeta::new(accounts.boss, true);

            self.accounts.onyc_mint = AccountMeta::new(accounts.onyc_mint, false);

            self.accounts.boss_onyc_account = AccountMeta::new(accounts.boss_onyc_account, false);

            self.accounts.mint_authority =
                AccountMeta::new_readonly(accounts.mint_authority, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.onyc_mint.clone());

            metas.push(self.accounts.boss_onyc_account.clone());

            metas.push(self.accounts.mint_authority.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: OfferVaultDeposit
    // ....................................................................

    /// Main instruction struct for OfferVaultDeposit
    pub struct OfferVaultDepositInstruction {
        pub accounts: OfferVaultDepositInstructionAccountMetas,
        pub data: OfferVaultDepositInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for OfferVaultDeposit instruction
    #[derive(Debug, Clone, Default)]
    pub struct OfferVaultDepositInstructionAccountMetas {
        pub vault_authority: AccountMeta,

        pub token_mint: AccountMeta,

        pub boss_token_account: AccountMeta,

        pub vault_token_account: AccountMeta,

        pub boss: AccountMeta,

        pub state: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for OfferVaultDeposit instruction
    #[derive(Debug, Clone)]
    pub struct OfferVaultDepositInstructionAccounts {
        pub vault_authority: Pubkey,

        pub token_mint: Pubkey,

        pub boss_token_account: Pubkey,

        pub vault_token_account: Pubkey,

        pub boss: Pubkey,

        pub state: Pubkey,

        pub token_program: Pubkey,
    }

    impl OfferVaultDepositInstructionAccounts {
        pub fn new(
            vault_authority: Pubkey,

            token_mint: Pubkey,

            boss_token_account: Pubkey,

            vault_token_account: Pubkey,

            boss: Pubkey,

            state: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                vault_authority,

                token_mint,

                boss_token_account,

                vault_token_account,

                boss,

                state,

                token_program,
            }
        }
    }

    /// Instruction data for OfferVaultDeposit
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct OfferVaultDepositInstructionData {
        pub amount: u64,
    }

    impl OfferVaultDepositInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for OfferVaultDepositInstruction
    impl OfferVaultDepositInstruction {
        fn discriminator() -> [u8; 8] {
            [69u8, 131u8, 100u8, 85u8, 82u8, 151u8, 72u8, 74u8]
        }

        pub fn data(data: OfferVaultDepositInstructionData) -> Self {
            Self {
                accounts: OfferVaultDepositInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: OfferVaultDepositInstructionAccounts) -> Self {
            self.accounts.vault_authority =
                AccountMeta::new_readonly(accounts.vault_authority, false);

            self.accounts.token_mint = AccountMeta::new_readonly(accounts.token_mint, false);

            self.accounts.boss_token_account = AccountMeta::new(accounts.boss_token_account, false);

            self.accounts.vault_token_account =
                AccountMeta::new(accounts.vault_token_account, false);

            self.accounts.boss = AccountMeta::new(accounts.boss, true);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.vault_authority.clone());

            metas.push(self.accounts.token_mint.clone());

            metas.push(self.accounts.boss_token_account.clone());

            metas.push(self.accounts.vault_token_account.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: OfferVaultWithdraw
    // ....................................................................

    /// Main instruction struct for OfferVaultWithdraw
    pub struct OfferVaultWithdrawInstruction {
        pub accounts: OfferVaultWithdrawInstructionAccountMetas,
        pub data: OfferVaultWithdrawInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for OfferVaultWithdraw instruction
    #[derive(Debug, Clone, Default)]
    pub struct OfferVaultWithdrawInstructionAccountMetas {
        pub vault_authority: AccountMeta,

        pub token_mint: AccountMeta,

        pub boss_token_account: AccountMeta,

        pub vault_token_account: AccountMeta,

        pub boss: AccountMeta,

        pub state: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for OfferVaultWithdraw instruction
    #[derive(Debug, Clone)]
    pub struct OfferVaultWithdrawInstructionAccounts {
        pub vault_authority: Pubkey,

        pub token_mint: Pubkey,

        pub boss_token_account: Pubkey,

        pub vault_token_account: Pubkey,

        pub boss: Pubkey,

        pub state: Pubkey,

        pub token_program: Pubkey,
    }

    impl OfferVaultWithdrawInstructionAccounts {
        pub fn new(
            vault_authority: Pubkey,

            token_mint: Pubkey,

            boss_token_account: Pubkey,

            vault_token_account: Pubkey,

            boss: Pubkey,

            state: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                vault_authority,

                token_mint,

                boss_token_account,

                vault_token_account,

                boss,

                state,

                token_program,
            }
        }
    }

    /// Instruction data for OfferVaultWithdraw
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct OfferVaultWithdrawInstructionData {
        pub amount: u64,
    }

    impl OfferVaultWithdrawInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for OfferVaultWithdrawInstruction
    impl OfferVaultWithdrawInstruction {
        fn discriminator() -> [u8; 8] {
            [230u8, 135u8, 129u8, 48u8, 138u8, 202u8, 20u8, 72u8]
        }

        pub fn data(data: OfferVaultWithdrawInstructionData) -> Self {
            Self {
                accounts: OfferVaultWithdrawInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: OfferVaultWithdrawInstructionAccounts) -> Self {
            self.accounts.vault_authority =
                AccountMeta::new_readonly(accounts.vault_authority, false);

            self.accounts.token_mint = AccountMeta::new_readonly(accounts.token_mint, false);

            self.accounts.boss_token_account = AccountMeta::new(accounts.boss_token_account, false);

            self.accounts.vault_token_account =
                AccountMeta::new(accounts.vault_token_account, false);

            self.accounts.boss = AccountMeta::new(accounts.boss, true);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.vault_authority.clone());

            metas.push(self.accounts.token_mint.clone());

            metas.push(self.accounts.boss_token_account.clone());

            metas.push(self.accounts.vault_token_account.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ProposeBoss
    // ....................................................................

    /// Main instruction struct for ProposeBoss
    pub struct ProposeBossInstruction {
        pub accounts: ProposeBossInstructionAccountMetas,
        pub data: ProposeBossInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ProposeBoss instruction
    #[derive(Debug, Clone, Default)]
    pub struct ProposeBossInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for ProposeBoss instruction
    #[derive(Debug, Clone)]
    pub struct ProposeBossInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl ProposeBossInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey) -> Self {
            Self { state, boss }
        }
    }

    /// Instruction data for ProposeBoss
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ProposeBossInstructionData {
        pub new_boss: Pubkey,
    }

    impl ProposeBossInstructionData {
        pub fn new(new_boss: Pubkey) -> Self {
            Self { new_boss }
        }
    }

    /// Implementation for ProposeBossInstruction
    impl ProposeBossInstruction {
        fn discriminator() -> [u8; 8] {
            [163u8, 199u8, 158u8, 47u8, 155u8, 78u8, 174u8, 173u8]
        }

        pub fn data(data: ProposeBossInstructionData) -> Self {
            Self {
                accounts: ProposeBossInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ProposeBossInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RemoveAdmin
    // ....................................................................

    /// Main instruction struct for RemoveAdmin
    pub struct RemoveAdminInstruction {
        pub accounts: RemoveAdminInstructionAccountMetas,
        pub data: RemoveAdminInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RemoveAdmin instruction
    #[derive(Debug, Clone, Default)]
    pub struct RemoveAdminInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for RemoveAdmin instruction
    #[derive(Debug, Clone)]
    pub struct RemoveAdminInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl RemoveAdminInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey) -> Self {
            Self { state, boss }
        }
    }

    /// Instruction data for RemoveAdmin
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RemoveAdminInstructionData {
        pub admin_to_remove: Pubkey,
    }

    impl RemoveAdminInstructionData {
        pub fn new(admin_to_remove: Pubkey) -> Self {
            Self { admin_to_remove }
        }
    }

    /// Implementation for RemoveAdminInstruction
    impl RemoveAdminInstruction {
        fn discriminator() -> [u8; 8] {
            [74u8, 202u8, 71u8, 106u8, 252u8, 31u8, 72u8, 183u8]
        }

        pub fn data(data: RemoveAdminInstructionData) -> Self {
            Self {
                accounts: RemoveAdminInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RemoveAdminInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RemoveApprover
    // ....................................................................

    /// Main instruction struct for RemoveApprover
    pub struct RemoveApproverInstruction {
        pub accounts: RemoveApproverInstructionAccountMetas,
        pub data: RemoveApproverInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RemoveApprover instruction
    #[derive(Debug, Clone, Default)]
    pub struct RemoveApproverInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for RemoveApprover instruction
    #[derive(Debug, Clone)]
    pub struct RemoveApproverInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl RemoveApproverInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey) -> Self {
            Self { state, boss }
        }
    }

    /// Instruction data for RemoveApprover
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RemoveApproverInstructionData {
        pub approver: Pubkey,
    }

    impl RemoveApproverInstructionData {
        pub fn new(approver: Pubkey) -> Self {
            Self { approver }
        }
    }

    /// Implementation for RemoveApproverInstruction
    impl RemoveApproverInstruction {
        fn discriminator() -> [u8; 8] {
            [214u8, 72u8, 133u8, 48u8, 50u8, 58u8, 227u8, 224u8]
        }

        pub fn data(data: RemoveApproverInstructionData) -> Self {
            Self {
                accounts: RemoveApproverInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RemoveApproverInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: SetKillSwitch
    // ....................................................................

    /// Main instruction struct for SetKillSwitch
    pub struct SetKillSwitchInstruction {
        pub accounts: SetKillSwitchInstructionAccountMetas,
        pub data: SetKillSwitchInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for SetKillSwitch instruction
    #[derive(Debug, Clone, Default)]
    pub struct SetKillSwitchInstructionAccountMetas {
        pub state: AccountMeta,

        pub signer: AccountMeta,
    }

    /// Account pubkeys for SetKillSwitch instruction
    #[derive(Debug, Clone)]
    pub struct SetKillSwitchInstructionAccounts {
        pub state: Pubkey,

        pub signer: Pubkey,
    }

    impl SetKillSwitchInstructionAccounts {
        pub fn new(state: Pubkey, signer: Pubkey) -> Self {
            Self { state, signer }
        }
    }

    /// Instruction data for SetKillSwitch
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SetKillSwitchInstructionData {
        pub enable: bool,
    }

    impl SetKillSwitchInstructionData {
        pub fn new(enable: bool) -> Self {
            Self { enable }
        }
    }

    /// Implementation for SetKillSwitchInstruction
    impl SetKillSwitchInstruction {
        fn discriminator() -> [u8; 8] {
            [228u8, 119u8, 172u8, 135u8, 209u8, 250u8, 172u8, 216u8]
        }

        pub fn data(data: SetKillSwitchInstructionData) -> Self {
            Self {
                accounts: SetKillSwitchInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SetKillSwitchInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.signer = AccountMeta::new_readonly(accounts.signer, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.signer.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: SetOnycMint
    // ....................................................................

    /// Main instruction struct for SetOnycMint
    pub struct SetOnycMintInstruction {
        pub accounts: SetOnycMintInstructionAccountMetas,
        pub data: SetOnycMintInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for SetOnycMint instruction
    #[derive(Debug, Clone, Default)]
    pub struct SetOnycMintInstructionAccountMetas {
        pub state: AccountMeta,

        pub boss: AccountMeta,

        pub onyc_mint: AccountMeta,
    }

    /// Account pubkeys for SetOnycMint instruction
    #[derive(Debug, Clone)]
    pub struct SetOnycMintInstructionAccounts {
        pub state: Pubkey,

        pub boss: Pubkey,

        pub onyc_mint: Pubkey,
    }

    impl SetOnycMintInstructionAccounts {
        pub fn new(state: Pubkey, boss: Pubkey, onyc_mint: Pubkey) -> Self {
            Self {
                state,

                boss,

                onyc_mint,
            }
        }
    }

    /// Instruction data for SetOnycMint
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SetOnycMintInstructionData {}

    impl SetOnycMintInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for SetOnycMintInstruction
    impl SetOnycMintInstruction {
        fn discriminator() -> [u8; 8] {
            [177u8, 83u8, 119u8, 179u8, 44u8, 141u8, 201u8, 24u8]
        }

        pub fn data(data: SetOnycMintInstructionData) -> Self {
            Self {
                accounts: SetOnycMintInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SetOnycMintInstructionAccounts) -> Self {
            self.accounts.state = AccountMeta::new(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self.accounts.onyc_mint = AccountMeta::new_readonly(accounts.onyc_mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.onyc_mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: TakeOffer
    // ....................................................................

    /// Main instruction struct for TakeOffer
    pub struct TakeOfferInstruction {
        pub accounts: TakeOfferInstructionAccountMetas,
        pub data: TakeOfferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for TakeOffer instruction
    #[derive(Debug, Clone, Default)]
    pub struct TakeOfferInstructionAccountMetas {
        pub offer: AccountMeta,

        pub state: AccountMeta,

        pub boss: AccountMeta,

        pub vault_authority: AccountMeta,

        pub vault_token_in_account: AccountMeta,

        pub vault_token_out_account: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_in_program: AccountMeta,

        pub token_out_mint: AccountMeta,

        pub token_out_program: AccountMeta,

        pub user_token_in_account: AccountMeta,

        pub user_token_out_account: AccountMeta,

        pub boss_token_in_account: AccountMeta,

        pub mint_authority: AccountMeta,

        pub instructions_sysvar: AccountMeta,

        pub user: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for TakeOffer instruction
    #[derive(Debug, Clone)]
    pub struct TakeOfferInstructionAccounts {
        pub offer: Pubkey,

        pub state: Pubkey,

        pub boss: Pubkey,

        pub vault_authority: Pubkey,

        pub vault_token_in_account: Pubkey,

        pub vault_token_out_account: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_in_program: Pubkey,

        pub token_out_mint: Pubkey,

        pub token_out_program: Pubkey,

        pub user_token_in_account: Pubkey,

        pub user_token_out_account: Pubkey,

        pub boss_token_in_account: Pubkey,

        pub mint_authority: Pubkey,

        pub user: Pubkey,
    }

    impl TakeOfferInstructionAccounts {
        pub fn new(
            offer: Pubkey,

            state: Pubkey,

            boss: Pubkey,

            vault_authority: Pubkey,

            vault_token_in_account: Pubkey,

            vault_token_out_account: Pubkey,

            token_in_mint: Pubkey,

            token_in_program: Pubkey,

            token_out_mint: Pubkey,

            token_out_program: Pubkey,

            user_token_in_account: Pubkey,

            user_token_out_account: Pubkey,

            boss_token_in_account: Pubkey,

            mint_authority: Pubkey,

            user: Pubkey,
        ) -> Self {
            Self {
                offer,

                state,

                boss,

                vault_authority,

                vault_token_in_account,

                vault_token_out_account,

                token_in_mint,

                token_in_program,

                token_out_mint,

                token_out_program,

                user_token_in_account,

                user_token_out_account,

                boss_token_in_account,

                mint_authority,

                user,
            }
        }
    }

    /// Instruction data for TakeOffer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct TakeOfferInstructionData {
        pub token_in_amount: u64,

        pub approval_message: Option<ApprovalMessage>,
    }

    impl TakeOfferInstructionData {
        pub fn new(token_in_amount: u64, approval_message: Option<ApprovalMessage>) -> Self {
            Self {
                token_in_amount,

                approval_message,
            }
        }
    }

    /// Implementation for TakeOfferInstruction
    impl TakeOfferInstruction {
        fn discriminator() -> [u8; 8] {
            [128u8, 156u8, 242u8, 207u8, 237u8, 192u8, 103u8, 240u8]
        }

        pub fn data(data: TakeOfferInstructionData) -> Self {
            Self {
                accounts: TakeOfferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: TakeOfferInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new(accounts.offer, false);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, false);

            self.accounts.vault_authority =
                AccountMeta::new_readonly(accounts.vault_authority, false);

            self.accounts.vault_token_in_account =
                AccountMeta::new(accounts.vault_token_in_account, false);

            self.accounts.vault_token_out_account =
                AccountMeta::new(accounts.vault_token_out_account, false);

            self.accounts.token_in_mint = AccountMeta::new(accounts.token_in_mint, false);

            self.accounts.token_in_program =
                AccountMeta::new_readonly(accounts.token_in_program, false);

            self.accounts.token_out_mint = AccountMeta::new(accounts.token_out_mint, false);

            self.accounts.token_out_program =
                AccountMeta::new_readonly(accounts.token_out_program, false);

            self.accounts.user_token_in_account =
                AccountMeta::new(accounts.user_token_in_account, false);

            self.accounts.user_token_out_account =
                AccountMeta::new(accounts.user_token_out_account, false);

            self.accounts.boss_token_in_account =
                AccountMeta::new(accounts.boss_token_in_account, false);

            self.accounts.mint_authority =
                AccountMeta::new_readonly(accounts.mint_authority, false);

            self.accounts.instructions_sysvar = AccountMeta::new_readonly(
                pubkey!("Sysvar1nstructions1111111111111111111111111"),
                false,
            );

            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.vault_authority.clone());

            metas.push(self.accounts.vault_token_in_account.clone());

            metas.push(self.accounts.vault_token_out_account.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_in_program.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.push(self.accounts.token_out_program.clone());

            metas.push(self.accounts.user_token_in_account.clone());

            metas.push(self.accounts.user_token_out_account.clone());

            metas.push(self.accounts.boss_token_in_account.clone());

            metas.push(self.accounts.mint_authority.clone());

            metas.push(self.accounts.instructions_sysvar.clone());

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: TakeOfferPermissionless
    // ....................................................................

    /// Main instruction struct for TakeOfferPermissionless
    pub struct TakeOfferPermissionlessInstruction {
        pub accounts: TakeOfferPermissionlessInstructionAccountMetas,
        pub data: TakeOfferPermissionlessInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for TakeOfferPermissionless instruction
    #[derive(Debug, Clone, Default)]
    pub struct TakeOfferPermissionlessInstructionAccountMetas {
        pub offer: AccountMeta,

        pub state: AccountMeta,

        pub boss: AccountMeta,

        pub vault_authority: AccountMeta,

        pub vault_token_in_account: AccountMeta,

        pub vault_token_out_account: AccountMeta,

        pub permissionless_authority: AccountMeta,

        pub permissionless_token_in_account: AccountMeta,

        pub permissionless_token_out_account: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_in_program: AccountMeta,

        pub token_out_mint: AccountMeta,

        pub token_out_program: AccountMeta,

        pub user_token_in_account: AccountMeta,

        pub user_token_out_account: AccountMeta,

        pub boss_token_in_account: AccountMeta,

        pub mint_authority: AccountMeta,

        pub instructions_sysvar: AccountMeta,

        pub user: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for TakeOfferPermissionless instruction
    #[derive(Debug, Clone)]
    pub struct TakeOfferPermissionlessInstructionAccounts {
        pub offer: Pubkey,

        pub state: Pubkey,

        pub boss: Pubkey,

        pub vault_authority: Pubkey,

        pub vault_token_in_account: Pubkey,

        pub vault_token_out_account: Pubkey,

        pub permissionless_authority: Pubkey,

        pub permissionless_token_in_account: Pubkey,

        pub permissionless_token_out_account: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_in_program: Pubkey,

        pub token_out_mint: Pubkey,

        pub token_out_program: Pubkey,

        pub user_token_in_account: Pubkey,

        pub user_token_out_account: Pubkey,

        pub boss_token_in_account: Pubkey,

        pub mint_authority: Pubkey,

        pub user: Pubkey,
    }

    impl TakeOfferPermissionlessInstructionAccounts {
        pub fn new(
            offer: Pubkey,

            state: Pubkey,

            boss: Pubkey,

            vault_authority: Pubkey,

            vault_token_in_account: Pubkey,

            vault_token_out_account: Pubkey,

            permissionless_authority: Pubkey,

            permissionless_token_in_account: Pubkey,

            permissionless_token_out_account: Pubkey,

            token_in_mint: Pubkey,

            token_in_program: Pubkey,

            token_out_mint: Pubkey,

            token_out_program: Pubkey,

            user_token_in_account: Pubkey,

            user_token_out_account: Pubkey,

            boss_token_in_account: Pubkey,

            mint_authority: Pubkey,

            user: Pubkey,
        ) -> Self {
            Self {
                offer,

                state,

                boss,

                vault_authority,

                vault_token_in_account,

                vault_token_out_account,

                permissionless_authority,

                permissionless_token_in_account,

                permissionless_token_out_account,

                token_in_mint,

                token_in_program,

                token_out_mint,

                token_out_program,

                user_token_in_account,

                user_token_out_account,

                boss_token_in_account,

                mint_authority,

                user,
            }
        }
    }

    /// Instruction data for TakeOfferPermissionless
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct TakeOfferPermissionlessInstructionData {
        pub token_in_amount: u64,

        pub approval_message: Option<ApprovalMessage>,
    }

    impl TakeOfferPermissionlessInstructionData {
        pub fn new(token_in_amount: u64, approval_message: Option<ApprovalMessage>) -> Self {
            Self {
                token_in_amount,

                approval_message,
            }
        }
    }

    /// Implementation for TakeOfferPermissionlessInstruction
    impl TakeOfferPermissionlessInstruction {
        fn discriminator() -> [u8; 8] {
            [37u8, 190u8, 224u8, 77u8, 197u8, 39u8, 203u8, 230u8]
        }

        pub fn data(data: TakeOfferPermissionlessInstructionData) -> Self {
            Self {
                accounts: TakeOfferPermissionlessInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: TakeOfferPermissionlessInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new(accounts.offer, false);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, false);

            self.accounts.vault_authority =
                AccountMeta::new_readonly(accounts.vault_authority, false);

            self.accounts.vault_token_in_account =
                AccountMeta::new(accounts.vault_token_in_account, false);

            self.accounts.vault_token_out_account =
                AccountMeta::new(accounts.vault_token_out_account, false);

            self.accounts.permissionless_authority =
                AccountMeta::new_readonly(accounts.permissionless_authority, false);

            self.accounts.permissionless_token_in_account =
                AccountMeta::new(accounts.permissionless_token_in_account, false);

            self.accounts.permissionless_token_out_account =
                AccountMeta::new(accounts.permissionless_token_out_account, false);

            self.accounts.token_in_mint = AccountMeta::new(accounts.token_in_mint, false);

            self.accounts.token_in_program =
                AccountMeta::new_readonly(accounts.token_in_program, false);

            self.accounts.token_out_mint = AccountMeta::new(accounts.token_out_mint, false);

            self.accounts.token_out_program =
                AccountMeta::new_readonly(accounts.token_out_program, false);

            self.accounts.user_token_in_account =
                AccountMeta::new(accounts.user_token_in_account, false);

            self.accounts.user_token_out_account =
                AccountMeta::new(accounts.user_token_out_account, false);

            self.accounts.boss_token_in_account =
                AccountMeta::new(accounts.boss_token_in_account, false);

            self.accounts.mint_authority =
                AccountMeta::new_readonly(accounts.mint_authority, false);

            self.accounts.instructions_sysvar = AccountMeta::new_readonly(
                pubkey!("Sysvar1nstructions1111111111111111111111111"),
                false,
            );

            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.vault_authority.clone());

            metas.push(self.accounts.vault_token_in_account.clone());

            metas.push(self.accounts.vault_token_out_account.clone());

            metas.push(self.accounts.permissionless_authority.clone());

            metas.push(self.accounts.permissionless_token_in_account.clone());

            metas.push(self.accounts.permissionless_token_out_account.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_in_program.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.push(self.accounts.token_out_program.clone());

            metas.push(self.accounts.user_token_in_account.clone());

            metas.push(self.accounts.user_token_out_account.clone());

            metas.push(self.accounts.boss_token_in_account.clone());

            metas.push(self.accounts.mint_authority.clone());

            metas.push(self.accounts.instructions_sysvar.clone());

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: TransferMintAuthorityToBoss
    // ....................................................................

    /// Main instruction struct for TransferMintAuthorityToBoss
    pub struct TransferMintAuthorityToBossInstruction {
        pub accounts: TransferMintAuthorityToBossInstructionAccountMetas,
        pub data: TransferMintAuthorityToBossInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for TransferMintAuthorityToBoss instruction
    #[derive(Debug, Clone, Default)]
    pub struct TransferMintAuthorityToBossInstructionAccountMetas {
        pub boss: AccountMeta,

        pub state: AccountMeta,

        pub mint: AccountMeta,

        pub mint_authority: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for TransferMintAuthorityToBoss instruction
    #[derive(Debug, Clone)]
    pub struct TransferMintAuthorityToBossInstructionAccounts {
        pub boss: Pubkey,

        pub state: Pubkey,

        pub mint: Pubkey,

        pub mint_authority: Pubkey,

        pub token_program: Pubkey,
    }

    impl TransferMintAuthorityToBossInstructionAccounts {
        pub fn new(
            boss: Pubkey,

            state: Pubkey,

            mint: Pubkey,

            mint_authority: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                boss,

                state,

                mint,

                mint_authority,

                token_program,
            }
        }
    }

    /// Instruction data for TransferMintAuthorityToBoss
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct TransferMintAuthorityToBossInstructionData {}

    impl TransferMintAuthorityToBossInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for TransferMintAuthorityToBossInstruction
    impl TransferMintAuthorityToBossInstruction {
        fn discriminator() -> [u8; 8] {
            [197u8, 61u8, 42u8, 52u8, 70u8, 93u8, 30u8, 125u8]
        }

        pub fn data(data: TransferMintAuthorityToBossInstructionData) -> Self {
            Self {
                accounts: TransferMintAuthorityToBossInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(
            mut self,
            accounts: TransferMintAuthorityToBossInstructionAccounts,
        ) -> Self {
            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.mint_authority =
                AccountMeta::new_readonly(accounts.mint_authority, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.mint_authority.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: TransferMintAuthorityToProgram
    // ....................................................................

    /// Main instruction struct for TransferMintAuthorityToProgram
    pub struct TransferMintAuthorityToProgramInstruction {
        pub accounts: TransferMintAuthorityToProgramInstructionAccountMetas,
        pub data: TransferMintAuthorityToProgramInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for TransferMintAuthorityToProgram instruction
    #[derive(Debug, Clone, Default)]
    pub struct TransferMintAuthorityToProgramInstructionAccountMetas {
        pub boss: AccountMeta,

        pub state: AccountMeta,

        pub mint: AccountMeta,

        pub mint_authority: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for TransferMintAuthorityToProgram instruction
    #[derive(Debug, Clone)]
    pub struct TransferMintAuthorityToProgramInstructionAccounts {
        pub boss: Pubkey,

        pub state: Pubkey,

        pub mint: Pubkey,

        pub mint_authority: Pubkey,

        pub token_program: Pubkey,
    }

    impl TransferMintAuthorityToProgramInstructionAccounts {
        pub fn new(
            boss: Pubkey,

            state: Pubkey,

            mint: Pubkey,

            mint_authority: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                boss,

                state,

                mint,

                mint_authority,

                token_program,
            }
        }
    }

    /// Instruction data for TransferMintAuthorityToProgram
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct TransferMintAuthorityToProgramInstructionData {}

    impl TransferMintAuthorityToProgramInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for TransferMintAuthorityToProgramInstruction
    impl TransferMintAuthorityToProgramInstruction {
        fn discriminator() -> [u8; 8] {
            [98u8, 112u8, 50u8, 135u8, 53u8, 6u8, 149u8, 232u8]
        }

        pub fn data(data: TransferMintAuthorityToProgramInstructionData) -> Self {
            Self {
                accounts: TransferMintAuthorityToProgramInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(
            mut self,
            accounts: TransferMintAuthorityToProgramInstructionAccounts,
        ) -> Self {
            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.mint_authority =
                AccountMeta::new_readonly(accounts.mint_authority, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.boss.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.mint_authority.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateOfferFee
    // ....................................................................

    /// Main instruction struct for UpdateOfferFee
    pub struct UpdateOfferFeeInstruction {
        pub accounts: UpdateOfferFeeInstructionAccountMetas,
        pub data: UpdateOfferFeeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateOfferFee instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateOfferFeeInstructionAccountMetas {
        pub offer: AccountMeta,

        pub token_in_mint: AccountMeta,

        pub token_out_mint: AccountMeta,

        pub state: AccountMeta,

        pub boss: AccountMeta,
    }

    /// Account pubkeys for UpdateOfferFee instruction
    #[derive(Debug, Clone)]
    pub struct UpdateOfferFeeInstructionAccounts {
        pub offer: Pubkey,

        pub token_in_mint: Pubkey,

        pub token_out_mint: Pubkey,

        pub state: Pubkey,

        pub boss: Pubkey,
    }

    impl UpdateOfferFeeInstructionAccounts {
        pub fn new(
            offer: Pubkey,

            token_in_mint: Pubkey,

            token_out_mint: Pubkey,

            state: Pubkey,

            boss: Pubkey,
        ) -> Self {
            Self {
                offer,

                token_in_mint,

                token_out_mint,

                state,

                boss,
            }
        }
    }

    /// Instruction data for UpdateOfferFee
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateOfferFeeInstructionData {
        pub new_fee_basis_points: u16,
    }

    impl UpdateOfferFeeInstructionData {
        pub fn new(new_fee_basis_points: u16) -> Self {
            Self {
                new_fee_basis_points,
            }
        }
    }

    /// Implementation for UpdateOfferFeeInstruction
    impl UpdateOfferFeeInstruction {
        fn discriminator() -> [u8; 8] {
            [254u8, 162u8, 70u8, 248u8, 117u8, 70u8, 197u8, 118u8]
        }

        pub fn data(data: UpdateOfferFeeInstructionData) -> Self {
            Self {
                accounts: UpdateOfferFeeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateOfferFeeInstructionAccounts) -> Self {
            self.accounts.offer = AccountMeta::new(accounts.offer, false);

            self.accounts.token_in_mint = AccountMeta::new_readonly(accounts.token_in_mint, false);

            self.accounts.token_out_mint =
                AccountMeta::new_readonly(accounts.token_out_mint, false);

            self.accounts.state = AccountMeta::new_readonly(accounts.state, false);

            self.accounts.boss = AccountMeta::new_readonly(accounts.boss, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.offer.clone());

            metas.push(self.accounts.token_in_mint.clone());

            metas.push(self.accounts.token_out_mint.clone());

            metas.push(self.accounts.state.clone());

            metas.push(self.accounts.boss.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

/// Custom struct: AdminAddedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AdminAddedEvent {
    pub admin: Pubkey,

    pub boss: Pubkey,
}

impl AdminAddedEvent {
    pub fn new(admin: Pubkey, boss: Pubkey) -> Self {
        Self { admin, boss }
    }
}

/// Custom struct: AdminRemovedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AdminRemovedEvent {
    pub admin: Pubkey,

    pub boss: Pubkey,
}

impl AdminRemovedEvent {
    pub fn new(admin: Pubkey, boss: Pubkey) -> Self {
        Self { admin, boss }
    }
}

/// Custom struct: AdminsClearedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AdminsClearedEvent {
    pub boss: Pubkey,
}

impl AdminsClearedEvent {
    pub fn new(boss: Pubkey) -> Self {
        Self { boss }
    }
}

/// Custom struct: ApprovalMessage
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ApprovalMessage {
    pub program_id: Pubkey,

    pub user_pubkey: Pubkey,

    pub expiry_unix: u64,
}

impl ApprovalMessage {
    pub fn new(program_id: Pubkey, user_pubkey: Pubkey, expiry_unix: u64) -> Self {
        Self {
            program_id,

            user_pubkey,

            expiry_unix,
        }
    }
}

/// Custom struct: ApproverAddedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ApproverAddedEvent {
    pub approver: Pubkey,

    pub boss: Pubkey,
}

impl ApproverAddedEvent {
    pub fn new(approver: Pubkey, boss: Pubkey) -> Self {
        Self { approver, boss }
    }
}

/// Custom struct: ApproverRemovedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ApproverRemovedEvent {
    pub approver: Pubkey,

    pub boss: Pubkey,
}

impl ApproverRemovedEvent {
    pub fn new(approver: Pubkey, boss: Pubkey) -> Self {
        Self { approver, boss }
    }
}

/// Custom struct: BossAcceptedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BossAcceptedEvent {
    pub old_boss: Pubkey,

    pub new_boss: Pubkey,
}

impl BossAcceptedEvent {
    pub fn new(old_boss: Pubkey, new_boss: Pubkey) -> Self {
        Self { old_boss, new_boss }
    }
}

/// Custom struct: BossProposedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BossProposedEvent {
    pub current_boss: Pubkey,

    pub proposed_boss: Pubkey,
}

impl BossProposedEvent {
    pub fn new(current_boss: Pubkey, proposed_boss: Pubkey) -> Self {
        Self {
            current_boss,

            proposed_boss,
        }
    }
}

/// Custom struct: GetAPYEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GetAPYEvent {
    pub offer_pda: Pubkey,

    pub apy: u64,

    pub apr: u64,

    pub timestamp: u64,
}

impl GetAPYEvent {
    pub fn new(offer_pda: Pubkey, apy: u64, apr: u64, timestamp: u64) -> Self {
        Self {
            offer_pda,

            apy,

            apr,

            timestamp,
        }
    }
}

/// Custom struct: GetCirculatingSupplyEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GetCirculatingSupplyEvent {
    pub circulating_supply: u64,

    pub total_supply: u64,

    pub vault_amount: u64,

    pub timestamp: u64,
}

impl GetCirculatingSupplyEvent {
    pub fn new(
        circulating_supply: u64,

        total_supply: u64,

        vault_amount: u64,

        timestamp: u64,
    ) -> Self {
        Self {
            circulating_supply,

            total_supply,

            vault_amount,

            timestamp,
        }
    }
}

/// Custom struct: GetNAVEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GetNAVEvent {
    pub offer_pda: Pubkey,

    pub current_price: u64,

    pub timestamp: u64,

    pub next_price_change_timestamp: u64,
}

impl GetNAVEvent {
    pub fn new(
        offer_pda: Pubkey,

        current_price: u64,

        timestamp: u64,

        next_price_change_timestamp: u64,
    ) -> Self {
        Self {
            offer_pda,

            current_price,

            timestamp,

            next_price_change_timestamp,
        }
    }
}

/// Custom struct: GetNavAdjustmentEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GetNavAdjustmentEvent {
    pub offer_pda: Pubkey,

    pub current_price: u64,

    pub previous_price: Option<u64>,

    pub adjustment: i64,

    pub timestamp: u64,
}

impl GetNavAdjustmentEvent {
    pub fn new(
        offer_pda: Pubkey,

        current_price: u64,

        previous_price: Option<u64>,

        adjustment: i64,

        timestamp: u64,
    ) -> Self {
        Self {
            offer_pda,

            current_price,

            previous_price,

            adjustment,

            timestamp,
        }
    }
}

/// Custom struct: GetTVLEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GetTVLEvent {
    pub offer_pda: Pubkey,

    pub tvl: u64,

    pub current_price: u64,

    pub token_supply: u64,

    pub timestamp: u64,
}

impl GetTVLEvent {
    pub fn new(
        offer_pda: Pubkey,

        tvl: u64,

        current_price: u64,

        token_supply: u64,

        timestamp: u64,
    ) -> Self {
        Self {
            offer_pda,

            tvl,

            current_price,

            token_supply,

            timestamp,
        }
    }
}

/// Custom struct: KillSwitchToggledEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct KillSwitchToggledEvent {
    pub enabled: bool,

    pub signer: Pubkey,
}

impl KillSwitchToggledEvent {
    pub fn new(enabled: bool, signer: Pubkey) -> Self {
        Self { enabled, signer }
    }
}

/// Custom struct: MaxSupplyConfiguredEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MaxSupplyConfiguredEvent {
    pub old_max_supply: u64,

    pub new_max_supply: u64,
}

impl MaxSupplyConfiguredEvent {
    pub fn new(old_max_supply: u64, new_max_supply: u64) -> Self {
        Self {
            old_max_supply,

            new_max_supply,
        }
    }
}

/// Custom struct: MintAuthorityTransferredToBossEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MintAuthorityTransferredToBossEvent {
    pub mint: Pubkey,

    pub old_authority: Pubkey,

    pub new_authority: Pubkey,
}

impl MintAuthorityTransferredToBossEvent {
    pub fn new(mint: Pubkey, old_authority: Pubkey, new_authority: Pubkey) -> Self {
        Self {
            mint,

            old_authority,

            new_authority,
        }
    }
}

/// Custom struct: MintAuthorityTransferredToProgramEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MintAuthorityTransferredToProgramEvent {
    pub mint: Pubkey,

    pub old_authority: Pubkey,

    pub new_authority: Pubkey,
}

impl MintAuthorityTransferredToProgramEvent {
    pub fn new(mint: Pubkey, old_authority: Pubkey, new_authority: Pubkey) -> Self {
        Self {
            mint,

            old_authority,

            new_authority,
        }
    }
}

/// Custom struct: ONycMintUpdatedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ONycMintUpdatedEvent {
    pub old_onyc_mint: Pubkey,

    pub new_onyc_mint: Pubkey,
}

impl ONycMintUpdatedEvent {
    pub fn new(old_onyc_mint: Pubkey, new_onyc_mint: Pubkey) -> Self {
        Self {
            old_onyc_mint,

            new_onyc_mint,
        }
    }
}

/// Custom struct: Offer
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Offer {
    pub token_in_mint: Pubkey,

    pub token_out_mint: Pubkey,

    pub vectors: [OfferVector; 10],

    pub fee_basis_points: u16,

    pub bump: u8,

    pub needs_approval: u8,

    pub allow_permissionless: u8,

    pub reserved: [u8; 131],
}

impl Offer {
    pub fn new(
        token_in_mint: Pubkey,

        token_out_mint: Pubkey,

        vectors: [OfferVector; 10],

        fee_basis_points: u16,

        bump: u8,

        needs_approval: u8,

        allow_permissionless: u8,

        reserved: [u8; 131],
    ) -> Self {
        Self {
            token_in_mint,

            token_out_mint,

            vectors,

            fee_basis_points,

            bump,

            needs_approval,

            allow_permissionless,

            reserved,
        }
    }
}

/// Custom struct: OfferClosedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferClosedEvent {
    pub offer_pda: Pubkey,

    pub boss: Pubkey,
}

impl OfferClosedEvent {
    pub fn new(offer_pda: Pubkey, boss: Pubkey) -> Self {
        Self { offer_pda, boss }
    }
}

/// Custom struct: OfferFeeUpdatedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferFeeUpdatedEvent {
    pub offer_pda: Pubkey,

    pub old_fee_basis_points: u16,

    pub new_fee_basis_points: u16,

    pub boss: Pubkey,
}

impl OfferFeeUpdatedEvent {
    pub fn new(
        offer_pda: Pubkey,

        old_fee_basis_points: u16,

        new_fee_basis_points: u16,

        boss: Pubkey,
    ) -> Self {
        Self {
            offer_pda,

            old_fee_basis_points,

            new_fee_basis_points,

            boss,
        }
    }
}

/// Custom struct: OfferMadeEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferMadeEvent {
    pub offer_pda: Pubkey,

    pub token_in_mint: Pubkey,

    pub token_out_mint: Pubkey,

    pub fee_basis_points: u16,

    pub boss: Pubkey,

    pub needs_approval: bool,

    pub allow_permissionless: bool,
}

impl OfferMadeEvent {
    pub fn new(
        offer_pda: Pubkey,

        token_in_mint: Pubkey,

        token_out_mint: Pubkey,

        fee_basis_points: u16,

        boss: Pubkey,

        needs_approval: bool,

        allow_permissionless: bool,
    ) -> Self {
        Self {
            offer_pda,

            token_in_mint,

            token_out_mint,

            fee_basis_points,

            boss,

            needs_approval,

            allow_permissionless,
        }
    }
}

/// Custom struct: OfferTakenEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferTakenEvent {
    pub offer_pda: Pubkey,

    pub token_in_amount: u64,

    pub token_out_amount: u64,

    pub fee_amount: u64,

    pub user: Pubkey,
}

impl OfferTakenEvent {
    pub fn new(
        offer_pda: Pubkey,

        token_in_amount: u64,

        token_out_amount: u64,

        fee_amount: u64,

        user: Pubkey,
    ) -> Self {
        Self {
            offer_pda,

            token_in_amount,

            token_out_amount,

            fee_amount,

            user,
        }
    }
}

/// Custom struct: OfferTakenPermissionlessEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferTakenPermissionlessEvent {
    pub offer_pda: Pubkey,

    pub token_in_amount: u64,

    pub token_out_amount: u64,

    pub fee_amount: u64,

    pub user: Pubkey,
}

impl OfferTakenPermissionlessEvent {
    pub fn new(
        offer_pda: Pubkey,

        token_in_amount: u64,

        token_out_amount: u64,

        fee_amount: u64,

        user: Pubkey,
    ) -> Self {
        Self {
            offer_pda,

            token_in_amount,

            token_out_amount,

            fee_amount,

            user,
        }
    }
}

/// Custom struct: OfferVaultDepositEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferVaultDepositEvent {
    pub mint: Pubkey,

    pub amount: u64,

    pub boss: Pubkey,
}

impl OfferVaultDepositEvent {
    pub fn new(mint: Pubkey, amount: u64, boss: Pubkey) -> Self {
        Self { mint, amount, boss }
    }
}

/// Custom struct: OfferVaultWithdrawEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferVaultWithdrawEvent {
    pub mint: Pubkey,

    pub amount: u64,

    pub boss: Pubkey,
}

impl OfferVaultWithdrawEvent {
    pub fn new(mint: Pubkey, amount: u64, boss: Pubkey) -> Self {
        Self { mint, amount, boss }
    }
}

/// Custom struct: OfferVector
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone, Default)]
pub struct OfferVector {
    pub start_time: u64,

    pub base_time: u64,

    pub base_price: u64,

    pub apr: u64,

    pub price_fix_duration: u64,
}

impl OfferVector {
    pub fn new(
        start_time: u64,

        base_time: u64,

        base_price: u64,

        apr: u64,

        price_fix_duration: u64,
    ) -> Self {
        Self {
            start_time,

            base_time,

            base_price,

            apr,

            price_fix_duration,
        }
    }
}

/// Custom struct: OfferVectorAddedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferVectorAddedEvent {
    pub offer_pda: Pubkey,

    pub start_time: u64,

    pub base_time: u64,

    pub base_price: u64,

    pub apr: u64,

    pub price_fix_duration: u64,
}

impl OfferVectorAddedEvent {
    pub fn new(
        offer_pda: Pubkey,

        start_time: u64,

        base_time: u64,

        base_price: u64,

        apr: u64,

        price_fix_duration: u64,
    ) -> Self {
        Self {
            offer_pda,

            start_time,

            base_time,

            base_price,

            apr,

            price_fix_duration,
        }
    }
}

/// Custom struct: OfferVectorDeletedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferVectorDeletedEvent {
    pub offer_pda: Pubkey,

    pub vector_start_time: u64,
}

impl OfferVectorDeletedEvent {
    pub fn new(offer_pda: Pubkey, vector_start_time: u64) -> Self {
        Self {
            offer_pda,

            vector_start_time,
        }
    }
}

/// Custom struct: OfferVectorRetiredEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OfferVectorRetiredEvent {
    pub offer_token_in_mint: Pubkey,

    pub offer_token_out_mint: Pubkey,

    pub vector_start_time: u64,
}

impl OfferVectorRetiredEvent {
    pub fn new(
        offer_token_in_mint: Pubkey,

        offer_token_out_mint: Pubkey,

        vector_start_time: u64,
    ) -> Self {
        Self {
            offer_token_in_mint,

            offer_token_out_mint,

            vector_start_time,
        }
    }
}

/// Custom struct: OnycTokensMintedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OnycTokensMintedEvent {
    pub onyc_mint: Pubkey,

    pub boss: Pubkey,

    pub amount: u64,
}

impl OnycTokensMintedEvent {
    pub fn new(onyc_mint: Pubkey, boss: Pubkey, amount: u64) -> Self {
        Self {
            onyc_mint,

            boss,

            amount,
        }
    }
}

/// Custom struct: PermissionlessAuthority
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PermissionlessAuthority {
    pub name: String,
}

impl PermissionlessAuthority {
    pub fn new(name: String) -> Self {
        Self { name }
    }
}

/// Custom struct: State
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct State {
    pub boss: Pubkey,

    pub proposed_boss: Pubkey,

    pub is_killed: bool,

    pub onyc_mint: Pubkey,

    pub admins: [Pubkey; 20],

    pub approver1: Pubkey,

    pub approver2: Pubkey,

    pub bump: u8,

    pub max_supply: u64,

    pub reserved: [u8; 128],
}

impl State {
    pub fn new(
        boss: Pubkey,

        proposed_boss: Pubkey,

        is_killed: bool,

        onyc_mint: Pubkey,

        admins: [Pubkey; 20],

        approver1: Pubkey,

        approver2: Pubkey,

        bump: u8,

        max_supply: u64,

        reserved: [u8; 128],
    ) -> Self {
        Self {
            boss,

            proposed_boss,

            is_killed,

            onyc_mint,

            admins,

            approver1,

            approver2,

            bump,

            max_supply,

            reserved,
        }
    }
}

/// Custom struct: StateClosedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct StateClosedEvent {
    pub state_pda: Pubkey,

    pub boss: Pubkey,
}

impl StateClosedEvent {
    pub fn new(state_pda: Pubkey, boss: Pubkey) -> Self {
        Self { state_pda, boss }
    }
}

// ============================================================================
// END OF GENERATED FILE
// ============================================================================
